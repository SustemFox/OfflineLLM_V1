# OfflineLLM_V1 — Текущий статус

> Последнее обновление: 26.06.2026 (Москва)
> Текущая ветка: `feature/modular-architecture`
> Состояние: рефакторинг прототипа в модульную архитектуру

## Что уже сделано
- Созданы domain-модели: `Message`, `LlmModel`, `DownloadState`.
- Созданы интерфейсы репозиториев: `LlmRepository`, `ModelRepository`.
- Реализованы фейковые репозитории:
  - `FakeLlmRepository` — имитирует потоковый ответ LLM.
  - `FakeModelRepository` — имитирует загрузку модели с прогрессом.
- Добавлена заглушка `LocalLlmRepository` для будущего настоящего LLM.
- Добавлен `AppProvider` — простая фабрика зависимостей.
- Добавлен `ChatViewModel` с `StateFlow` и управлением состоянием UI.
- Добавлен `OfflineLlmTheme` с кастомным primary-цветом.
- Добавлен анимированный `TypingIndicator`.
- Обновлён `app/build.gradle`: добавлены ViewModel Compose и корутины.
- Создан `PLAN.md` и `TODO.md`.

## Что сейчас не работает / не завершено
- `MainActivity.kt` всё ещё содержит старый нерабочий монолитный код.
  - `ChatApp()` вызывается без параметров, внутри есть `ChatApp()` без аргументов и незаконченный state.
- Отсутствуют `ChatScreen`, `SettingsScreen`.
- Нет навигации между экранами.
- Нет сохранения истории и настроек.

## Ближайшие задачи (по приоритету)
1. Полностью переписать `MainActivity.kt` — оставить только `setContent` + `OfflineLlmTheme` + `AppNavigation()`.
2. Создать `ChatScreen` с `LazyColumn` сообщений, полем ввода и кнопкой отправки.
3. Создать `SettingsScreen` с выбором цвета и модели + кнопкой загрузки.
4. Добавить `Navigation` (Compose Navigation) между чатом и настройками.
5. Собрать приложение и проверить, что фейковый режим работает.

## Блокеры
- Нет.

## Идеи на будущее
- Подключить `llama.cpp` через JNI для запуска .gguf моделей.
- Использовать MediaPipe LLM Inference API (более простой путь для Android).
- Добавить Room для истории сообщений.
- Добавить DataStore для настроек темы.
- Экран импорта модели из файловой системы устройства.
- Поддержка нескольких чатов.

## Как с этим работать с другого устройства
1. `git clone` репозиторий, переключиться на ветку `feature/modular-architecture`.
2. Прочитать этот файл и `TODO.md`.
3. Взять первую невыполненную задачу из раздела «Ближайшие задачи» выше.
4. После завершения задачи обновить `STATUS.md` и `TODO.md`, закоммитить.
